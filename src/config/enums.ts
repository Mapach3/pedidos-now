export enum ClientRoutes {
  HOME = "/",
  LOGIN = "/login",
  RESTAURANTS = "/restaurants/:location",
  REGISTER = "/register",
  PROFILE = "/profile",
  RESTAURANT_MENU = "/restaurantMenu/:titulo"
}
